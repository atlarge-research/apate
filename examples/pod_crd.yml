apiVersion: apate.opendc.org/v1
kind: PodConfiguration
metadata:
    name: crd-deployment
spec:
    tasks:
        -   timestamp: 1m
            relative_to_pod: true
            state:
                create_pod_response: ERROR

